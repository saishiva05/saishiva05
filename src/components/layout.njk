<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>
            {% if title %}
                {{ title }}{% else %}SS Overseas{% endif %}
        </title>
        <link rel="icon" href="{{ 'favicon.ico' | url | safe }}">
        <link rel="stylesheet" href="{{ '/assets/styles.css' | url | safe }}">
    </head>
    <body class="has-navbar-fixed-top">
        {{ content | safe }}
    </body>
</html>